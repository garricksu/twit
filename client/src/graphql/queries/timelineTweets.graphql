query TimelineTweets($cursor: String) {
  timelineTweets(cursor: $cursor) {
    tweets {
      id
      textContent
      createdAt
      user {
        id
        username
        firstName
        lastName
        email
      }
    }
    hasMore
  }
}
