query TimelineTweets($cursor: String) {
  timelineTweets(cursor: $cursor) {
    tweets {
      id
      textContent
      createdAt
      likeCount
      liked
      user {
        id
        username
        firstName
        lastName
        email
      }
    }
    hasMore
  }
}
